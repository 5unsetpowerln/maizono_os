[package]
name = "bootloader"
version = "0.1.0"
edition = "2021"

[dependencies]
uefi = { version = "0.32.0", features = ["global_allocator"] }
log = "0.4.22"
spin = "0.9.8"
anyhow = { version = "1.0.89", default-features = false }
# anyhow = { version = "1.0" }
